using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceJunk : MonoBehaviour
{
    // constructor
    public SpaceJunk(double pXValue, double pYValue, int pPointValue, bool pIsEasterEgg) {

    }
    
    // Start is called before the first frame update
    void Start()
    {
        // randomize the asset from prefab space junk
        // grab its x and y values, point value, and easter egg
        // where to start it?
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
